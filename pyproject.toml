[project]
name = "forecast-mlops-handson"
version = "0.1.0"
description = "需要予測 × MLOps ハンズオン (Databricks) のサンプルデータ生成"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
